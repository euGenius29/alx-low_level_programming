#include "main.h"

/**
 * mul - this multiplies two given digits
 *
 * @a: first digit to munltiply with @b
 * @b: second digit to multiply with @a
 *
 * Return: the answer given
 */

int mul(int a, int b)
{
	int ans;

	ans = a * b;
	return (ans);
}
